# Task Sync dashboard

This is the React frontend for the task sync project.

## Start it

From this folder:

```powershell
npm install
npm run dev
```

The page expects the backend at `http://localhost:5000`. Set `VITE_API_URL` if the backend is running somewhere else.

## Useful commands

```powershell
npm run build
npm run lint
```
