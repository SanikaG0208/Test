{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node -e \"require('dotenv').config(); const { Pool } = require('pg'); const { migrate } = require('./migrate'); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); migrate(pool).then(() => pool.end()).then(() => console.log('Migrations applied')).catch((error) => { console.error(error); process.exit(1); })\"",
    "test": "node --test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.23.0"
  }
}
